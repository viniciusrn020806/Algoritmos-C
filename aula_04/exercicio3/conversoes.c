float celci_fahre(float celsius){
   float resultado =  (celsius * 9.0 / 5.0) + 32; 
    return resultado;
}

int minutos_segundos(int minutos){
    int resultado = (minutos * 60);
    return resultado;
}
